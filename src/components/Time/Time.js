import React from 'react';
// import Bookmark from '../Bookmark/Bookmark';

const Time = ({time}) => {
    console.log(time)
    let mainTime = 0;
    if(time) mainTime = time;
    else mainTime = 0;

    return (
        <div>
           
        </div>
    );
};

export default Time;